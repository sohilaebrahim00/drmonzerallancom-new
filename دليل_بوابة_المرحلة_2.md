# دليل تنفيذ بوابة المرحلة ٢ — خطوة بخطوة

> ده الدليل التشغيلي بالعربي. النسخة الإنجليزية `PHASE_2_GATE_RUNBOOK.md` هي المرجع اللي Claude Code
> بيقراه — نفس الخطوات بالظبط. لو غيّرتي حاجة في واحد، قولي عشان أظبّط التاني.

**معرّف مشروع Supabase بتاعك:** `nkvycfmxabtwmoirrjxv`
**رابط الـ webhook اللي هتحتاجيه في الخطوة ٤:**
```
https://nkvycfmxabtwmoirrjxv.supabase.co/functions/v1/stripe-webhook
```

الوقت المتوقع: ساعة لساعة ونص، أغلبها انتظار نشر.

---

## الخطوة ٠ — تأكدي إن الأدوات جاهزة

افتحي terminal في مجلد المشروع واعملي:

```
supabase --version
supabase projects list
```

- لو `supabase` مش متعرّف → نصّبيه: `npm install -g supabase` (أو `scoop install supabase`).
- لو `projects list` طلب تسجيل دخول → `supabase login` (هيفتح المتصفح).
- في نتيجة `projects list` لازم تلاقي `nkvycfmxabtwmoirrjxv` وجنبه علامة إنه linked. لو مش linked:
  `supabase link --project-ref nkvycfmxabtwmoirrjxv`

كمان جهّزي:
- تبويب Stripe Dashboard مفتوح
- تبويب Supabase Dashboard مفتوح
- تبويب Netlify مفتوح
- **إيميل حقيقي تقدري تفتحيه** — دعوة إنشاء الحساب هتيجي عليه

---

## الخطوة ١ — منتجات الاختبار في Stripe

**١.١** في Stripe، فوق على الشمال فيه مفتاح **Test mode** — شغّليه. المفروض شريط برتقالي/بنفسجي يظهر
مكتوب عليه "Test mode". لو مش شغال، كل اللي جاي هيتعمل على فلوس حقيقية.

**١.٠ الأول — عندك المنتجات دي فعلاً؟ وفي أنهي وضع؟**

المنتجات **منفصلة بين الوضعين**: منتج الـ live مش موجود في test والعكس. والاتنين شكلهم `prod_…`،
فالـ id لوحده مش بيقول حاجة.

للتأكد: شغّلي مفتاح **Test mode**، وبعدين افتحي **Products**.
- لقيتي الستة → يبقى دول منتجات test، عدّي على ١.٣ وانسخي الـ ids.
- القايمة فاضية أو فيها منتجات تانية → منتجاتك في live بس، ولازم تعملي ٦ للاختبار.

اقفلي Test mode وبصي تاني عشان تتأكدي إيه الموجود في live.

**١.٢** من القايمة: **Product catalogue → Products → + Add product**

اعملي ٦ منتجات، واحد واحد. الأسماء **بالظبط** كده — ده النص اللي العميل بيقراه في صفحة الدفع:

| الاسم | السعر | النوع |
|---|---|---|
| Diet Basic | 49.00 USD | One-off |
| Diet Plus | 89.00 USD | One-off |
| Diet Premium | 119.00 USD | One-off |
| Treatment Basic | 119.00 USD | One-off |
| Treatment Plus | 169.00 USD | One-off |
| Treatment Premium | 199.00 USD | One-off |

> **تصحيح ٢٢ أغسطس.** النسخة الأولى من الدليل كانت بتقول إن سعر المنتج في Stripe لازم يطابق الكود.
> ده **غلط** في المشروع ده. دالة `create-consultation-checkout-session` بتبني سعر لحظي وقت الدفع:
> ```ts
> price_data: { currency: "usd", product: productId, unit_amount: def.amountCents }
> ```
> المنتج بيدّي **الاسم والوصف** اللي بيظهروا في صفحة Stripe بس. أي Price مربوط بالمنتج مش بيتقري
> خالص. يعني المبلغ اللي العميل يشوفه ويدفعه جاي كله من `amountCents` في الدالة — بند 2.5.1 —
> والسعر اللي بتكتبيه وانتي بتعملي المنتج شكلي.
>
> اكتبيه صح برضه عشان لوحة Stripe متضلّلكيش بعدين، بس **المنتجات الموجودة عندك مش محتاجة تعديل سعر
> عشان الخصم يبقى صح.**

⚠️ **مهم:** اختاري **One-off** مش Recurring. دي باقات مرة واحدة مش اشتراك شهري.

**١.٣** محتاجة الـ **Product ID** بتاع كل منتج — بيبدأ بـ `prod_`، **مش** الـ Price ID
(اللي بيبدأ `price_`). الكود بيستخدم الـ Product ID.

أسرع طريقة تجيبي الستة مرة واحدة بدل ما تفتحي كل منتج لوحده:
- في صفحة **Product catalog** اضغطي زرار **Edit columns** (فوق على اليمين) وفعّلي عمود **ID** →
  هيظهر جنب كل منتج في الجدول ونسخيهم على طول.
- أو زرار **Export products** → ملف CSV فيه الأسماء والـ ids.

اكتبي الستة في ملف مؤقت عندك بالشكل ده:
```
DIET_BASIC       = prod_...
DIET_PLUS        = prod_...
DIET_PREMIUM     = prod_...
TREATMENT_BASIC  = prod_...
TREATMENT_PLUS   = prod_...
TREATMENT_PREMIUM= prod_...
```

**١.٤** كمان من **Developers → API keys** (وانتي لسه في Test mode) انسخي:
- **Secret key** — بيبدأ `sk_test_`
- **Publishable key** — بيبدأ `pk_test_`

---

## الخطوة ٢ — الأسرار في Supabase

في الـ terminal، مكان `...` حطي القيم الحقيقية:

```
supabase secrets set STRIPE_SECRET_KEY=sk_test_...
supabase secrets set STRIPE_PRODUCT_DIET_BASIC=prod_...
supabase secrets set STRIPE_PRODUCT_DIET_PLUS=prod_...
supabase secrets set STRIPE_PRODUCT_DIET_PREMIUM=prod_...
supabase secrets set STRIPE_PRODUCT_TREATMENT_BASIC=prod_...
supabase secrets set STRIPE_PRODUCT_TREATMENT_PLUS=prod_...
supabase secrets set STRIPE_PRODUCT_TREATMENT_PREMIUM=prod_...
supabase secrets set SITE_URL=https://monzerallan.com
```

**١** `SITE_URL` بقى **إجباري** بعد إصلاح المرحلة ١ (بند 1.5). من غيره الشيك آوت بيرجّع 500.

⚠️ الستة `STRIPE_PRODUCT_*` لازم يكونوا ids بتوع **test** طول فترة الاختبار، ويرجعوا لـ **live**
في الخطوة ٧. لو حطيتي id بتاع live وانتي في وضع test، الشيك آوت هيرجّع
"This program package isn't available yet". **اكتبي ids الـ live عندك قبل ما تستبدليهم.**

**٢** تأكدي إن دول موجودين برضه (لو مش موجودين حطيهم):
```
supabase secrets list
```
لازم تلاقي: `GEMINI_API_KEY`، `RESEND_API_KEY`، `EMAIL_FROM`، `ADMIN_NOTIFICATION_EMAIL`.
`supabase secrets list` بيعرض الأسماء وبصمة القيمة بس — مش بيعرض السر نفسه. **متلصقيش أي سر هنا في الشات.**

---

## الخطوة ٣ — النشر بالترتيب ده بالظبط

⚠️ الترتيب مش اختياري. `PHASE_J` بيسحب صلاحية القراءة الكاملة عن جدول `profiles`، والنسخة
المنشورة حاليًا من الموقع لسه بتطلب عمود `is_admin`. لو طبّقتي الـ SQL الأول، الموقع الحي هيقع
لحد ما البيلد الجديد ينزل.

### ٣أ — الفرونت إند الأول

**١** في Netlify → Site configuration → Environment variables:
غيّري `VITE_STRIPE_PUBLISHABLE_KEY` لمفتاح **الاختبار** (`pk_test_...`).
> اكتبي القيمة القديمة (`pk_live_...`) في ملفك المؤقت — هترجّعيها في الخطوة ٧.

**٢** ابني وانشري:
```
npm run build:web
```
وارفعي `dist/` على Netlify (أو `git push` لو الموقع مربوط بالريبو — بس الكوميتات لسه محلية، فلازم
`git push` الأول).

**٣** افتحي `https://monzerallan.com` وتأكدي إنها بتفتح عادي. لو وقعت، وقفي هنا وقوليلي.

### ٣ب — بعدين الـ SQL

**١** Supabase Dashboard → **SQL Editor** → New query
**٢** افتحي `supabase/PHASE_J_FIXES_MIGRATION.sql` من مجلد المشروع، انسخي كل محتواه، الصقيه، و **Run**
**٣** لازم ينتهي من غير خطأ. لو ظهر خطأ، انسخي نصه وقوليلي — الملف مصمّم إنه يتشغّل أكتر من مرة
بأمان، فمفيش ضرر من إعادة المحاولة.
**٤** ارجعي للموقع، سجّلي دخول، وافتحي `/account`. لو الصفحة اشتغلت عادي يبقى تغيير صلاحيات
`profiles` تمام.

### ٣ج — وأخيرًا الدوال

```
supabase functions deploy stripe-webhook --no-verify-jwt
supabase functions deploy create-consultation-checkout-session
supabase functions deploy create-checkout-session
supabase functions deploy ai-chat
supabase functions deploy food-scan
supabase functions deploy food-search
supabase functions deploy contact-submit
supabase functions deploy create-consultation
supabase functions deploy get-availability
supabase functions deploy admin-availability
supabase functions deploy delete-account
```

كل أمر بياخد ٢٠-٤٠ ثانية. لو واحد فشل، انسخي الخطأ وقوليلي قبل ما تكمّلي.

`--no-verify-jwt` على الـ webhook ضروري — Stripe مش بتبعت توكن، ومن غير الفلاج دي Supabase هترفض
كل الأحداث بـ 401.

---

## الخطوة ٤ — تسجيل الـ webhook (أكتر خطوة الناس بتغلط فيها)

**٤.١** Stripe (لسه في Test mode) → **Developers → Webhooks → + Add endpoint**

**٤.٢** في خانة **Endpoint URL** الصقي:
```
https://nkvycfmxabtwmoirrjxv.supabase.co/functions/v1/stripe-webhook
```

**٤.٣** اضغطي **Select events** واختاري **١٢ حدث** — دول بالظبط اللي الكود بيتعامل معاهم:

باقات المرة الواحدة:
```
checkout.session.completed
checkout.session.async_payment_succeeded
checkout.session.async_payment_failed
charge.refunded
charge.dispute.created
```

الاشتراكات الشهرية ودورة الدفع:
```
customer.subscription.created
customer.subscription.updated
customer.subscription.deleted
invoice.paid
invoice.payment_failed
payment_intent.succeeded
payment_intent.payment_failed
```

المجموعة التانية مش هتشتغل في اختبار النهاردة، بس **لازم** تكون مسجّلة لما ترجعي للـ live —
من غير `invoice.paid` الاشتراك الشهري ميرجعش `active` بعد التجديد، والأعضاء الحاليين بيقعوا واحد
واحد من غير ما حد ياخد باله.

**٤.٤** احفظي، وبعدين اضغطي على الـ endpoint اللي اتعمل → **Signing secret → Reveal**.
انسخي القيمة (بتبدأ `whsec_`).

**٤.٥** حطيها كسر **وأعيدي النشر**:
```
supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_...
supabase functions deploy stripe-webhook --no-verify-jwt
```

> 🔴 **إعادة النشر دي إجبارية.** الدوال بتقرا الأسرار وقت التشغيل الأول بس، فالنسخة الشغالة
> هتفضل ماسكة القيمة القديمة أو الفاضية. لو نسيتيها، كل حدث هيفشل بـ
> `400 signature verification failed` — واللي بيبان بالظبط زي السر الغلط، فهتفضلي تدوّري في المكان
> الغلط. **دي الغلطة الأولى اللي بتحصل في الخطوة دي.**

**٤.٦** اختبار سريع: في صفحة الـ endpoint في Stripe اضغطي **Send test webhook** واختاري
`checkout.session.completed`.
- **200** أو **400** → تمام، الدالة بتشتغل وبتتحقق من التوقيع
- **401** → الـ `--no-verify-jwt` مأخدتش. أعيدي `supabase functions deploy stripe-webhook --no-verify-jwt`

---

## الخطوة ٤.٥ — ربط Google Meet (شرط للخطوة ٥.٥)

الخطوتين ٥.٥ و ٥.٦ (احجزي موعد، بعدين الغيه) **مش هيشتغلوا** من غير أسرار Google.
`create-consultation` بتفحص `isGoogleCalendarConfigured()` في أولها وبترفض الحجز وترجّع الرصيد لو
مش متظبطة — فالعميل يقرا "Online scheduling is being activated…" ومش بيقدر يحجز.

اعملي **`دليل_ربط_جوجل_ميت.md`** كله قبل ما تكمّلي. محتاجة حساب Google بتاع الدكتور، ونص ساعة.

لو حبيتي تفصلي: تقدري تعملي خطوة ٥.١ لـ ٥.٤ (الشراء والتأكد من الرصيد ٦) من غير Google، وتأجّلي
٥.٥ و ٥.٦ لحد ما الربط يخلص. بس **البوابة متعتبرش مكتملة** غير لما الحجز والإلغاء يتجرّبوا.

---

## الخطوة ٥ — شراء باقتين على نفس الحساب

⚠️ **اتنين مش واحدة.** الشراية الواحدة مش هتكشف بند 2.8 (تجميع الرصيد عبر أكتر من صف) أبدًا.

> **اتحدّث بعد المرحلة ٢.٥** — عدد الجلسات بقى ٢/٣/٤ بدل ١/٢/٣، فالأرقام اللي تحت مختلفة عن
> النسخة القديمة من الدليل.

**٥.١** افتحي `https://monzerallan.com/packages` واشتري **Diet Premium** (**٤** استشارات).
بيانات البطاقة:
```
رقم البطاقة:  4242 4242 4242 4242
تاريخ:        أي تاريخ في المستقبل (مثلاً 12/30)
CVC:          أي ٣ أرقام
الرمز البريدي: أي رقم
```
حطي **الإيميل الحقيقي** اللي جهّزتيه.

**٥.٢** افتحي إيميلك — هتلاقي دعوة لإنشاء حساب. اقبليها، اعملي باسورد، وسجّلي دخول.

**٥.٣** **وانتي مسجّلة دخول وبنفس الإيميل**، ارجعي `/packages` واشتري **Treatment Basic**
(**٢** استشارات).

**٥.٤** افتحي `/account`. **لازم تشوفي ٦ استشارات.**
لو شفتي **٢** (يعني آخر شراية بس) → بند 2.8 مأخدش، وقفي وقوليلي.

**٥.٥** افتحي `/account/consultations` واحجزي موعد. الرصيد لازم ينزل لـ **٥**.

**٥.٦** الغي الحجز. الرصيد لازم يرجع **٦** — ده بند 2.9.

**٥.٧** (اختياري، دقيقة) افتحي نسخة التطبيق وشوفي حساب `diet_premium` بيعرض اسم الباقة ولا
"No active membership". الفشل هنا **متوقع** — ده بند C1 في المرحلة ٣. بس قوليلي شفتي إيه.

---

## الخطوة ٦ — جمع الدليل

```
supabase functions logs stripe-webhook
```

وبعدين قوليلي **"خلصت"** وأنا هسحب صفوف `payments` و `subscriptions` و `consultation_credits`
وأجمّع سجل البوابة.

**الشكل الصح:**
- `payments` — صفين، `status = 'succeeded'`، وعدد الاستشارات **٤ و ٢**
- `subscriptions` — صفين، نفس الـ `user_id`، `active`، الحدود **٤ و ٢**
- `consultation_credits` — صفين موجبين مجموعهم **٦**
- اللوجات — من غير `42P10`، من غير "Failed to upsert"، من غير 401
- إيميل ترحيب **واحد** لكل شراية، مش تلاتة (بند 2.7)

---

## الخطوة ٧ — الرجوع للوضع الحقيقي (متنسيهاش)

بعد ما البوابة تعدّي:

**١** امسحي صفوف الاختبار — في SQL Editor:
```sql
delete from public.consultation_credits
  where payment_id in (select id from public.payments
                       where stripe_session_id like 'cs_test_%');
delete from public.subscriptions
  where stripe_checkout_session_id like 'cs_test_%';
delete from public.payments
  where stripe_session_id like 'cs_test_%';
```
(الترتيب ده مهم عشان المفاتيح الأجنبية.) والحساب التجريبي نفسه امسحيه من
Supabase → Authentication → Users.

**٢** Stripe → اقفلي **Test mode** (ارجعي Live).

**٣** أعيدي إنشاء الـ ٦ منتجات في **Live** لو مش موجودين، وحدّثي أسرار `STRIPE_PRODUCT_*` بالـ ids
الحقيقية.

**٤** `supabase secrets set STRIPE_SECRET_KEY=sk_live_...`

**٥** سجّلي endpoint جديد في **Live** بنفس الـ URL و **نفس الـ ١٢ حدث**، خدي الـ `whsec_` بتاعه:
```
supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_...
supabase functions deploy stripe-webhook --no-verify-jwt
```

**٦** Netlify → رجّعي `VITE_STRIPE_PUBLISHABLE_KEY` لـ `pk_live_...` وأعيدي النشر.

بس كده المتجر يبقى مفتوح فعلاً.

---

## لو حاجة وقعت

| اللي بيحصل | السبب | الحل |
|---|---|---|
| كل الأحداث 401 | `--no-verify-jwt` ناقص | أعيدي نشر الـ webhook بالفلاج |
| `400 signature verification failed` | حطيتي السر وما أعدتيش النشر | `supabase functions deploy stripe-webhook --no-verify-jwt` |
| الشيك آوت بيرجّع 500 فورًا | `SITE_URL` مش متحطّ | خطوة ٢ |
| "This package isn't available yet" | سر `STRIPE_PRODUCT_*` ناقص، أو فيه id بتاع live وانتي في test | خطوة ١.٣ و ٢ |
| `42P10` في اللوجات | `PHASE_J` ما اتطبّقش أو اتطبّق نص | أعيدي خطوة ٣ب |
| اتخصم ومفيش أي صف | الإيميل بحروف كبيرة/صغيرة مختلفة | بند 2.5 — ابعتيلي اللوج |
| `/account` بيعرض ١ بعد شرايتين | تعديل 2.8 مش في البيلد المنشور | تأكدي إن خطوة ٣أ نشرت آخر كود |
| الموقع وقع بعد الـ SQL | نشرتي الـ SQL قبل الفرونت | انشري الفرونت حالًا، الترتيب في خطوة ٣ |

في أي حالة: انسخي نص الخطأ زي ما هو وابعتيه — **من غير أي مفاتيح**.
