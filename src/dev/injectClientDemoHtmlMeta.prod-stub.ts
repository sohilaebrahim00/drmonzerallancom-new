/**
 * PRODUCTION STUB for src/dev/injectClientDemoHtmlMeta.ts — see
 * demoFixtures.prod-stub.ts for the full rationale. Swapped in only for the
 * two real production builds. injectAppHtmlMeta.ts never actually calls
 * this there (isClientDemoBuild() is always false), but the stub removes
 * even the literal "Client Preview" title string from the bundle.
 */
export function injectClientDemoHtmlMeta() {
  /* no-op in production */
}
