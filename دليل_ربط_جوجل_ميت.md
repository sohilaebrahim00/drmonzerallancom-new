# ربط Google Meet — خطوة بخطوة

> **ليه دي مهمة:** من غير الأسرار الأربعة دي، **الحجز مش بيشتغل خالص**. مش بس اللينك ناقص —
> `create-consultation` بترفض الحجز من أوله وبترجّع للعميل:
> *"Online scheduling is being activated… live meeting scheduling is not yet connected."*
> يعني عميل دفع وخد رصيد ومش هيقدر يحجز. ودي كمان بتوقّف الخطوة ٥.٥ في بوابة المرحلة ٢.

**محتاجة إيه قبل ما تبدأي:** وصول لحساب Google بتاع الدكتور — المواعيد هتنزل في تقويمه هو،
والتفويض لازم يتم بحسابه. اقعدي معاه أو خليه يعمل خطوة ٤ بنفسه.

الوقت المتوقع: نص ساعة.

---

## الخطوة ١ — مشروع Google Cloud وتفعيل الـ API

**١.١** ادخلي [console.cloud.google.com](https://console.cloud.google.com) بحساب الدكتور.

**١.٢** فوق على الشمال، من قائمة المشاريع → **New Project**. سمّيه مثلاً `monzerallan-booking`.
لو عنده مشروع قديم يقدر يستخدمه عادي.

**١.٣** من البحث فوق اكتبي **Google Calendar API** → افتحيها → **Enable**.

⚠️ من غير الخطوة دي كل الطلبات هترجع `403 API not enabled`.

---

## الخطوة ٢ — شاشة الموافقة (OAuth consent screen)

دي أخطر خطوة في الدليل كله، وفيها فخ بيوقّع الحجز بعد أسبوع من الإطلاق.

**٢.١** من القائمة الجانبية → **APIs & Services → OAuth consent screen**.

**٢.٢ اختاري نوع المستخدم:**

- عند الدكتور **Google Workspace** (إيميل على دومينه هو) → اختاري **Internal**. دي الأنضف: مفيش
  تحذيرات ومفيش انتهاء صلاحية.
- إيميل **Gmail عادي** → اختاري **External**.

**٢.٣** املي البيانات الأساسية: اسم التطبيق، إيميل الدعم، إيميل المطوّر. مش محتاجة لوجو ولا دومين
موثّق.

**٢.٤ — الفخ.** لو اخترتي **External**، التطبيق بيبقى في وضع **Testing** افتراضيًا. وفي الوضع ده:

> **الـ refresh token بينتهي بعد ٧ أيام.**

يعني كل حاجة هتشتغل تمام، وبعد أسبوع الحجز يقف فجأة من غير أي تغيير في الكود، ومحدش يعرف ليه.

**الحل:** في نفس الصفحة اضغطي **PUBLISH APP** وأكّدي. التطبيق يبقى في وضع **In production** والتوكن
يفضل شغال.

Google هتقول إن التطبيق محتاج مراجعة (verification) — **ده مش مطلوب في حالتكم.** المراجعة بتلزم
للتطبيقات المفتوحة للجمهور. هنا المستخدم الوحيد هو الدكتور نفسه. التطبيق غير المراجَع مسموح له
بـ ١٠٠ مستخدم، وهيظهر تحذير مرة واحدة وقت التفويض بس (خطوة ٤) — عادي، اضغطي **Advanced** →
**Go to … (unsafe)**.

---

## الخطوة ٣ — إنشاء OAuth client

**٣.١** **APIs & Services → Credentials → + Create Credentials → OAuth client ID**

**٣.٢** النوع: **Web application**

**٣.٣** تحت **Authorized redirect URIs** اضغطي **+ ADD URI** والصقي بالظبط:

```
https://developers.google.com/oauthplayground
```

⚠️ من غير النقطة دي، خطوة ٤ هترجّع `redirect_uri_mismatch`. اكتبيها بالحرف، من غير `/` في الآخر.

**٣.٤** احفظي، وانسخي **Client ID** و **Client secret**. دول أول سرّين من الأربعة.

---

## الخطوة ٤ — استخراج الـ refresh token

**٤.١** افتحي [developers.google.com/oauthplayground](https://developers.google.com/oauthplayground)

**٤.٢** من الترس ⚙️ فوق على اليمين، فعّلي **Use your own OAuth credentials**، والصقي الـ Client ID
والـ Client secret من الخطوة ٣.

**٤.٣** في القايمة على الشمال (Step 1)، بدل ما تدوّري، الصقي الـ scope ده في خانة
*"Input your own scopes"*:

```
https://www.googleapis.com/auth/calendar.events
```

اضغطي **Authorize APIs**.

**٤.٤** هيطلب تسجيل دخول — **لازم بحساب الدكتور**، مش حسابك. ولو ظهر تحذير "Google hasn't verified
this app" اضغطي **Advanced → Go to … (unsafe)** وبعدين **Continue**.

**٤.٥** في Step 2 اضغطي **Exchange authorization code for tokens**.

**٤.٦** هيظهرلك **Refresh token** — انسخيه. ده تالت سرّ.

> **لو الخانة طلعت فاضية:** معناها إن الحساب ده فوّض التطبيق قبل كده، وGoogle مش بتعيد إصدار
> refresh token في الحالة دي. الحل: ادخلي
> [myaccount.google.com/permissions](https://myaccount.google.com/permissions) بحساب الدكتور،
> امسحي التطبيق من القايمة، وأعيدي ٤.٣.

---

## الخطوة ٥ — الـ Calendar ID

رابع سرّ هو **إيميل حساب Google بتاع الدكتور** نفسه، مثلاً `dr.monzer@gmail.com`.

⚠️ **متكتبيش `primary`.** الكود بيستخدم القيمة دي لحاجتين: التقويم اللي الحدث هينزل فيه، **و**
إيميل الدكتور كمشارك في الاجتماع. `primary` بتنفع للتقويم بس وبتكسر المشارك.

---

## الخطوة ٦ — حطي الأسرار وأعيدي النشر

```
supabase secrets set GOOGLE_CLIENT_ID=...
supabase secrets set GOOGLE_CLIENT_SECRET=...
supabase secrets set GOOGLE_REFRESH_TOKEN=...
supabase secrets set GOOGLE_CALENDAR_ID=dr.monzer@gmail.com

supabase functions deploy create-consultation
```

> 🔴 **إعادة النشر إجبارية** — زي بالظبط ما حصل مع `STRIPE_WEBHOOK_SECRET`. الدوال بتقرا الأسرار
> وقت التشغيل الأول بس، فالنسخة الشغالة هتفضل شايفة الأسرار فاضية وترفض الحجز.

---

## الخطوة ٧ — التأكد

سجّلي دخول بحساب عنده رصيد استشارات، وافتحي `/account/consultations` واحجزي موعد.

**الشكل الصح:**

- الحجز يتم، والرصيد ينقص واحد
- يظهر لينك **Google Meet** في تفاصيل الحجز
- الميعاد ينزل في **تقويم الدكتور** أوتوماتيك
- **العميل يستلم دعوة تقويم على إيميله** — الكود بيبعت `sendUpdates=all`، فجوجل هي اللي بتبعت
  الدعوة والتذكيرات، مش إحنا

النقطة الأخيرة دي هي اللي كنا بنتكلم عنها في موضوع التذكيرات: الدعوة بتوصل على الإيميل، والميعاد
بينزل في تقويم العميل، وموبايله بيفكّره لوحده. من غير ما نبني أي جدولة.

جرّبي الإلغاء كمان — الحدث لازم يتشال من التقويم والرصيد يرجع.

---

## لو حاجة وقعت

| اللي بيحصل | السبب | الحل |
|---|---|---|
| `redirect_uri_mismatch` | الـ URI في خطوة ٣.٣ مش مطابق | الصقيه بالحرف، من غير `/` في الآخر |
| خانة الـ refresh token فاضية | الحساب فوّض قبل كده | امسحي التطبيق من myaccount.google.com/permissions وأعيدي |
| `403 API not enabled` | Calendar API مش مفعّلة | خطوة ١.٣ |
| `401` من Google بعد أسبوع | التطبيق لسه في وضع Testing | **PUBLISH APP** في خطوة ٢.٤، وأعيدي استخراج التوكن |
| الحجز لسه بيقول "not yet connected" | نسيتي إعادة النشر | `supabase functions deploy create-consultation` |
| الحدث بينزل من غير لينك Meet | الـ scope ناقص | جرّبي `https://www.googleapis.com/auth/calendar` بدل `calendar.events` |
| العميل مش بيستلم دعوة | إيميله غلط في الحساب | اتأكدي من `auth.users.email` بتاعه |

في أي حالة: انسخي نص الخطأ زي ما هو وابعتيه — **من غير أي أسرار**.
